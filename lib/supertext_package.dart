library supertext_package;

import 'package:flutter/material.dart';

class SupertextPackage extends StatelessWidget {
  const SupertextPackage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Super Package'),
      ),
    );
  }
}
